spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/t734070824/springcloud.git
          search-paths: git-config
server:
  port: 7001